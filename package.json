{
  "name": "mdxml-tools",
  "version": "0.0.1",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test-render-site": "npm run build && node build/render.js examples/onepage/page2.xml",
    "build": "esbuild src/*.ts --outdir=build",
    "build-css": "cp resources/style.css build",
    "prep": "rm -rf build && mkdir build && ln -sf resources/style.css build/style.css",
    "tsc": "tsc",
    "test": "vitest run --dir src",
    "release-patch": "npm run build && npm version patch && npm publish"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@rgrove/parse-xml": "^4.1.0",
    "@types/node": "^22.5.3",
    "fast-xml-parser": "^4.5.0",
    "josh_js_util": "^0.1.25"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4",
    "vitest": "^1.4.0"
  }
}
